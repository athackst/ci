# This file is generated from athackst/ci/template/.github/workflows/automerge.yml.jinja.
# Do not edit directly; update the template instead.

name: Automerge

on:
  workflow_run:
    workflows:
      - Labeler
    types:
      - completed

  pull_request_target:
    types:
      - labeled
      - unlabeled

jobs:
  pr-automerge:
    permissions:
      contents: write
      pull-requests: write
      checks: read
    uses: athackst/ci/.github/workflows/automerge.yml@main
    secrets:
      token: ${{ secrets.CI_BOT_TOKEN }}
